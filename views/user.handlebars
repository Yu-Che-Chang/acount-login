<div class="container">
  <div class="mt-5 d-flex flex-column align-items-center">
    <h5>Welcome back,{{userName}}</h5>
    <form action="/logout" method="get" class="d-flex flex-column">
      <button type="submit" class="btn btn-primary mt-3">登出</button>
    </form>
  </div>
</div>